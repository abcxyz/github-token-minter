# abcxyz/readability.yaml
- 
  if: |-
    assertion.workflow_ref.startsWith("abcxyz/readability/.github/workflows/terraform.yml") &&
    assertion.event_name == 'push' && 
    assertion.ref == 'refs/head/main'
  repositories: []
  permissions:
    members: 'write'
